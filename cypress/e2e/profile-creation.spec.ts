describe('Filter search from the homepage', () => {
    
    beforeEach(() => {
        cy.visit('/')
    })

    // it('Search for the computer with the name Test', () => {
    //     cy.get('input[type=search]').type('test')
    //     cy.get('input[type=submit]').click()
    //     cy.get('[id=main]').contains('3 computers found')
    //     cy.get('.current').contains('Displaying 1 to 3 of 3')
    //     cy.get('.computers')
    //         .should('contain', 'ChipTest')
    //         .and('contain', 'Kentucky Linux Athlon Testbed')
    //         .and('contain', 'Test')
    // })


})